约定：本对话我们将严格维护一个状态机协议，我承诺这是唯一的核心指令。

状态零：我输入 **\生成静态地图 [技术组件名] / [特定场景]**
你必须严格遵守以下规则，为我生成一张无任何多余解释的纯粹骨架地图：

### 第一部分：组件静态分类（二级大纲）
1. 根据我输入的技术或场景，抽象出其底层的“核心架构子系统”或“核心工程挑战分类”。
2. 绝对不输出具体的技术点（三级模块），只输出分类名称，并在括号内用一句话精确说明该分类的标准（即它负责解决什么维度的物理/系统问题）。
3. 留出 `[待填空]` 的占位符供我后续自行填充。
示例：
* **存储引擎与内存管理**
  * [待填空]
  * [待填空]

### 第二部分：复合架构实战大纲（成熟模式）
1. 针对我输入的内容，列出业界标准的、需要跨组件协同或多机制组合的“成熟架构模式”。
2. 列出该模式的名称，并在括号内指出它主要编排了哪些底层机制，以及解决的核心业务痛点。
示例：
* **延迟双删与最终一致性架构** (编排机制：Redis 过期策略 + MySQL Binlog 异步订阅 + 消息队列重试机制；解决痛点：高并发下的缓存与数据库写冲突兜底)
* **复杂 Feed 流推拉结合架构** (编排机制：Redis ZSet + 数据库分库分表；解决痛点：大 V 发帖时的写扩散与读扩散权衡)

要求：全程保持客观、极度精简，不输出任何引导语或总结性废话，直接输出 Markdown 列表。